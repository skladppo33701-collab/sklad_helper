import 'package:flutter/material.dart';

class SchedulePlannerDemoScreen extends StatelessWidget {
  const SchedulePlannerDemoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Home / Schedule (demo)')));
  }
}
